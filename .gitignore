# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python

# Trained models — only gen2_models is committed (selectively, see below)
models/
gen1_models/
gen1_logs/

# Inside gen2_models, commit only the best/best_model.zip files.
# Final snapshots and any other intermediates are ignored.
gen2_models/**/ppo_*_final.zip
gen2_models/**/resume.zip
gen2_models/**/*.broken
gen2_models/**/*.tmp

# Logs and wandb
logs/
wandb/
*.tfevents.*

# IDE / OS
.vscode/
.idea/
.DS_Store
*.swp